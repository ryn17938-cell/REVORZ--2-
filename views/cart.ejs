<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Keranjang Belanja</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/global.min.css">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #fdfbfb,  #f9fdff, #b8dcff, #85dcff);
            min-height: 100vh;
        }

        /* Confirmation Popup */
        .confirmation-popup {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 10000;
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.3s ease, visibility 0.3s ease;
        }
        .confirmation-popup.show {
            opacity: 1;
            visibility: visible;
        }
        .confirmation-popup-content {
            background: #ffffff;
            border-radius: 12px;
            padding: 32px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            text-align: center;
            max-width: 400px;
            width: 90%;
            transform: scale(0.8);
            transition: transform 0.3s ease;
        }
        .confirmation-popup.show .confirmation-popup-content {
            transform: scale(1);
        }
        .confirmation-popup-content h3 {
            font-size: 1.5rem;
            font-weight: 700;
            color: #2d3748;
            margin-bottom: 16px;
        }
        .confirmation-popup-content p {
            font-size: 1rem;
            color: #4a5568;
            margin-bottom: 24px;
        }
        .confirmation-popup-buttons {
            display: flex;
            justify-content: center;
            gap: 16px;
        }
        .confirmation-popup-buttons button {
            padding: 10px 20px;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
        }
        .confirmation-popup-buttons .btn-yes {
            background: #ef4444;
            color: white;
            border: none;
        }
        .confirmation-popup-buttons .btn-yes:hover {
            background: #dc2626;
        }
        .confirmation-popup-buttons .btn-no {
            background: #e2e8f0;
            color: #4a5568;
            border: 1px solid #cbd5e1;
        }
        .confirmation-popup-buttons .btn-no:hover {
            background: #cbd5e1;
        }

        /* Custom Popup Notification (reusing from edit.ejs for consistency) */
        .custom-popup {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) scale(0.8);
            padding: 24px 32px;
            border-radius: 12px;
            color: white;
            font-weight: 600;
            z-index: 10000;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 16px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s ease-in-out;
        }
        .custom-popup.show {
            opacity: 1;
            visibility: visible;
            transform: translate(-50%, -50%) scale(1);
        }
        .custom-popup.success {
            background: linear-gradient(135deg, #2dd4bf, #14b8a6);
        }
        .custom-popup.error {
            background: linear-gradient(135deg, #f87171, #ef4444);
        }
        .custom-popup .icon {
            width: 48px;
            height: 48px;
        }
        .custom-popup .message {
            font-size: 1.125rem;
            text-align: center;
        }
    </style>
</head>
<body class="font-sans text-gray-800 min-h-screen flex flex-col">

    <!-- Header -->
    <header class="bg-white bg-opacity-90 shadow-lg py-4 backdrop-blur-md">
        <div class="container mx-auto flex justify-between items-center px-4">
            <!-- Logo -->
            <a href="/" class="flex items-center space-x-3 text-3xl font-bold text-blue-600 hover:text-blue-700 transition duration-300">
                <img src="/image/Logo.png" alt="REVOZ Logo" class="logo h-16 w-16 object-contain">
                <span>REVOZ</span>
            </a>

            <!-- Navigation -->
            <nav class="hidden md:flex space-x-6 ml-8">
                <a href="/" class="text-gray-600 hover:text-blue-600 transition duration-300">Beranda</a>
                <a href="/catalog" class="text-gray-600 hover:text-blue-600 transition duration-300">Katalog</a>
                <a href="/category" class="text-gray-600 hover:text-blue-600 transition duration-300">Kategori</a>
                <a href="/about" class="text-gray-600 hover:text-blue-600 transition duration-300">Tentang Kami</a>
                <a href="/contact" class="text-gray-600 hover:text-blue-600 transition duration-300">Kontak</a>
                <% if (role === 'admin') { %>
                    <a href="/admin/add-product" class="text-gray-600 hover:text-blue-600 transition duration-300">Tambah Produk</a>
                <% } %>
            </nav>

            <!-- Search Bar -->
            <div class="flex-1 flex justify-center px-8">
                <form action="/category" method="get" class="w-full max-w-lg">
                    <div class="relative">
                        <span class="absolute inset-y-0 left-0 pl-3 flex items-center">
                            <svg class="h-5 w-5 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z" clip-rule="evenodd" />
                            </svg>
                        </span>
                        <input class="search-input w-full pl-10 pr-4 py-2 rounded-full text-gray-700 border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500" type="text" name="q" placeholder="Cari produk...">
                    </div>
                </form>
            </div>

            <!-- Utility Icons -->
            <div class="flex items-center space-x-4">
                <a href="/cart" class="relative text-blue-600 font-semibold border-b-2 border-blue-600 transition duration-300">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 0a2 2 0 100 4 2 2 0 000-4z" />
                    </svg>
                    <span class="absolute -top-2 -right-2 bg-red-500 text-white text-xs rounded-full px-1"><%= cartItemCount %></span>
                </a>
                <% if (isLoggedIn) { %>
                <div class="flex items-center space-x-2">
                    <% if (profilePicture) { %>
                    <img src="<%= profilePicture %>" alt="Profile Picture" class="w-8 h-8 rounded-full object-cover">
                    <% } else { %>
                    <div class="w-8 h-8 bg-blue-500 rounded-full flex items-center justify-center text-white font-bold">
                        <%= username.charAt(0).toUpperCase() %>
                    </div>
                    <% } %>
                    <a href="/profile" class="text-gray-600 hover:text-blue-600 font-medium transition duration-300">Halo, <%= username %></a>
                    <a href="/logout" class="text-red-600 hover:text-red-700 font-medium transition duration-300">Logout</a>
                </div>
                <% } else { %>
                <a href="/login" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full transition duration-300">Masuk</a>
                <% } %>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="flex-grow container mx-auto py-12 px-4">
        <div class="text-center mb-10">
            <h1 class="text-5xl font-extrabold text-gray-800 tracking-tight">Keranjang Belanja Anda</h1>
            <p class="mt-3 text-lg text-gray-600">Tinjau item Anda dan lanjutkan ke pembayaran.</p>
        </div>

        <% if (items && items.length) { %>
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 items-start">
                <!-- Cart Items -->
                <div class="lg:col-span-2 space-y-4">
                    <% let total = 0; %>
                    <% items.forEach(function(it){ %>
                        <% let subtotal = (it.price || 0) * (it.quantity || 1); total += subtotal; %>
                        <div class="bg-white/80 backdrop-blur-sm rounded-xl shadow-lg p-4 flex items-center space-x-4 transition-transform transform hover:scale-[1.02] duration-300">
                            <img src="<%= it.image || '/image/katalog.png' %>" alt="<%= it.name %>" class="w-24 h-24 object-cover rounded-lg flex-shrink-0">
                            <div class="flex-grow">
                                <h3 class="font-bold text-lg text-gray-800"><%= it.name %></h3>
                                <p class="text-sm text-gray-500">Warna: <%= it.warna %>, Ukuran: <%= it.ukuran %></p>
                                <p class="text-sm text-gray-600">Harga: Rp <%= it.price ? it.price.toLocaleString('id-ID') : '0' %></p>
                                    <div class="flex items-center mt-2">
                                        <span class="text-sm text-gray-600 mr-3">Kuantitas:</span>
                                        <div class="flex items-center border rounded-lg">
                                            <button class="quantity-btn minus px-3 py-1 text-gray-600 hover:bg-gray-100 rounded-l-lg transition-colors duration-200" 
                                                    data-item-id="<%= it.id %>" 
                                                    data-action="decrease">−</button>
                                            <input type="number" 
                                                   class="quantity-input w-16 px-2 py-1 text-center border-x" 
                                                   value="<%= it.quantity %>" 
                                                   min="1" 
                                                   data-item-id="<%= it.id %>">
                                            <button class="quantity-btn plus px-3 py-1 text-gray-600 hover:bg-gray-100 rounded-r-lg transition-colors duration-200" 
                                                    data-item-id="<%= it.id %>" 
                                                    data-action="increase">+</button>
                                        </div>
                                    </div>
                            </div>
                            <div class="text-right flex-shrink-0">
                                <p class="font-bold text-lg text-blue-600">Rp <%= subtotal.toLocaleString('id-ID') %></p>
                                <button data-item-id="<%= it.id %>" class="remove-item text-red-500 hover:text-red-700 text-sm font-medium mt-2 transition-colors duration-200">
                                    Hapus
                                </button>
                            </div>
                        </div>
                    <% }) %>
                </div>

                <!-- Order Summary -->
                <div class="lg:col-span-1">
                    <div class="bg-white/80 backdrop-blur-sm rounded-xl shadow-lg p-6 sticky top-24">
                        <h2 class="text-2xl font-bold border-b pb-4 mb-4">Ringkasan Pesanan</h2>
                        <div class="space-y-2 mb-4">
                            <div class="flex justify-between">
                                <span>Subtotal</span>
                                <span>Rp <%= total.toLocaleString('id-ID') %></span>
                            </div>
                            <div class="flex justify-between text-gray-600">
                                <span>Biaya Pengiriman</span>
                                <span class="font-medium text-green-600">Gratis</span>
                            </div>
                            <div class="flex justify-between font-bold text-xl border-t pt-4 mt-4">
                                <span>Total</span>
                                <span>Rp <%= total.toLocaleString('id-ID') %></span>
                            </div>
                        </div>
                        <button id="checkout" class="btn-primary w-full py-3">
                            Lanjut ke Pembayaran
                        </button>
                        <button id="clear-cart" class="w-full mt-3 text-sm text-gray-600 hover:text-red-600 transition duration-300">
                            Kosongkan Keranjang
                        </button>
                    </div>
                </div>
            </div>
        <% } else { %>
            <div class="bg-white/80 backdrop-blur-sm shadow-xl rounded-2xl p-12 text-center">
                <svg class="mx-auto h-16 w-16 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 0a2 2 0 100 4 2 2 0 000-4z" />
                </svg>
                <h2 class="mt-4 text-2xl font-bold text-gray-800">Keranjang Anda Kosong</h2>
                <p class="mt-2 text-gray-600">Sepertinya Anda belum menambahkan produk apa pun.</p>
                <a href="/catalog" class="mt-6 inline-block bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-full transition duration-300 transform hover:scale-105 shadow-lg">
                    Mulai Belanja
                </a>
            </div>
        <% } %>
    </main>

    <!-- Footer -->
    <footer class="site-footer bg-gray-800 text-white py-8 mt-auto">
        <div class="container mx-auto text-center px-4">
            <p>&copy; 2025 REVOZ. Semua Hak Dilindungi.</p>
        </div>
    </footer>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Get references to popup elements
                // Quantity Management
                document.querySelectorAll('.quantity-btn').forEach(btn => {
                    btn.addEventListener('click', async (e) => {
                        const itemId = btn.dataset.itemId;
                        const action = btn.dataset.action;
                        const input = btn.parentElement.querySelector('.quantity-input');
                        let currentQty = parseInt(input.value);

                        if (action === 'increase') {
                            currentQty++;
                        } else if (action === 'decrease' && currentQty > 1) {
                            currentQty--;
                        }

                        const r = await postJson('/api/cart/update-quantity', {
                            item_id: itemId,
                            quantity: currentQty
                        });

                        if (r && r.ok) {
                            input.value = currentQty;
                            showCustomPopup('Kuantitas berhasil diperbarui', true);
                            setTimeout(() => {
                                location.reload();
                            }, 1000);
                        } else {
                            showCustomPopup(r.message || 'Gagal memperbarui kuantitas', false);
                        }
                    });
                });

                document.querySelectorAll('.quantity-input').forEach(input => {
                    input.addEventListener('change', async (e) => {
                        const itemId = input.dataset.itemId;
                        let newQty = parseInt(input.value);

                        if (newQty < 1) {
                            newQty = 1;
                            input.value = 1;
                        }

                        const r = await postJson('/api/cart/update-quantity', {
                            item_id: itemId,
                            quantity: newQty
                        });

                        if (r && r.ok) {
                            showCustomPopup('Kuantitas berhasil diperbarui', true);
                            setTimeout(() => {
                                location.reload();
                            }, 1000);
                        } else {
                            showCustomPopup(r.message || 'Gagal memperbarui kuantitas', false);
                        }
                    });
                });

            const confirmationPopup = document.getElementById('confirmation-popup');
            const confirmYesBtn = document.getElementById('confirm-yes');
            const confirmNoBtn = document.getElementById('confirm-no');

            const checkoutPopup = document.getElementById('checkout-popup');
            const closeCheckoutBtn = document.getElementById('close-checkout-btn');
            const confirmPaymentBtn = document.getElementById('confirm-payment-btn');


            const customPopup = document.getElementById('custom-popup');
            const customPopupIcon = customPopup.querySelector('.icon');
            const customPopupMessage = customPopup.querySelector('.message');

            async function postJson(url, body) {
                const res = await fetch(url, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(body)
                });
                return res.json();
            }

            function showCustomPopup(message, success = true) {
                customPopupMessage.textContent = message;
                customPopup.classList.remove('success', 'error');
                customPopupIcon.innerHTML = ''; // Clear previous icon

                if (success) {
                    customPopup.classList.add('success');
                    customPopupIcon.innerHTML = `<svg class="w-12 h-12" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>`;
                } else {
                    customPopup.classList.add('error');
                    customPopupIcon.innerHTML = `<svg class="w-12 h-12" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>`;
                }

                customPopup.classList.add('show');

                setTimeout(() => {
                    customPopup.classList.remove('show');
                }, 3000); // Hide after 3 seconds
            }

            document.querySelectorAll('.remove-item').forEach(btn => {
                btn.addEventListener('click', async (e) => {
                    const id = btn.dataset.itemId;
                    const r = await postJson('/api/cart/remove', { item_id: id });
                    if (r && r.ok) {
                        showCustomPopup('Produk berhasil dihapus dari keranjang', true);
                        setTimeout(() => {
                            location.reload();
                        }, 1500);
                    } else {
                        showCustomPopup('Gagal menghapus item', false);
                    }
                });
            });

            document.getElementById('clear-cart')?.addEventListener('click', () => {
                confirmationPopup.classList.add('show');
            });

            // Open checkout popup
            document.getElementById('checkout')?.addEventListener('click', () => {
                checkoutPopup.classList.add('show');
            });

            closeCheckoutBtn.addEventListener('click', () => {
                checkoutPopup.classList.remove('show');
            });

            const paymentGroups = document.querySelectorAll('input[name="payment-group"]');
            const eWalletOptions = document.getElementById('e-wallet-options');
            const virtualAccountOptions = document.getElementById('virtual-account-options');

            paymentGroups.forEach(radio => {
                radio.addEventListener('change', () => {
                    if (radio.value === 'e-wallet') {
                        eWalletOptions.classList.remove('hidden');
                        virtualAccountOptions.classList.add('hidden');
                    } else {
                        eWalletOptions.classList.add('hidden');
                        virtualAccountOptions.classList.remove('hidden');
                    }
                });
            });

            
        });
    </script>

    <!-- Confirmation Popup -->
    <div id="confirmation-popup" class="confirmation-popup">
        <div class="confirmation-popup-content">
            <h3>Konfirmasi</h3>
            <p>Apakah Anda yakin ingin mengosongkan seluruh keranjang?</p>
            <div class="confirmation-popup-buttons">
                <button id="confirm-yes" class="btn-yes">Ya</button>
                <button id="confirm-no" class="btn-no">Tidak</button>
            </div>
        </div>
    </div>

    <!-- Checkout Popup -->
    <div id="checkout-popup" class="confirmation-popup">
        <div class="confirmation-popup-content max-h-screen overflow-y-auto" style="max-width: 650px; text-align: left;">
            <div class="flex justify-between items-center mb-6">
                <h3 class="text-2xl font-bold">Detail Pembayaran</h3>
                <button id="close-checkout-btn" class="text-gray-500 hover:text-gray-800">&times;</button>
            </div>
               <!-- Shipping Address Form -->
               <div class="mb-8 border-b pb-6">
                   <h4 class="font-bold text-lg mb-4">Alamat Pengiriman</h4>
                   <div class="space-y-4">
                       <div class="grid grid-cols-2 gap-4">
                           <div>
                               <label class="block text-sm font-medium text-gray-700 mb-1">Nama Penerima</label>
                               <input type="text" id="recipient-name" class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" placeholder="Nama lengkap penerima">
                           </div>
                           <div>
                               <label class="block text-sm font-medium text-gray-700 mb-1">No. Telepon</label>
                               <input type="tel" id="recipient-phone" class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" placeholder="Nomor telepon aktif">
                           </div>
                       </div>
                       <div>
                           <label class="block text-sm font-medium text-gray-700 mb-1">Alamat Lengkap</label>
                           <textarea id="shipping-address" rows="3" class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" placeholder="Nama jalan, nomor rumah, RT/RW"></textarea>
                       </div>
                       <div class="grid grid-cols-2 gap-4">
                           <div>
                               <label class="block text-sm font-medium text-gray-700 mb-1">Kota/Kabupaten</label>
                               <input type="text" id="city" class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" placeholder="Nama kota">
                           </div>
                           <div>
                               <label class="block text-sm font-medium text-gray-700 mb-1">Kode Pos</label>
                               <input type="text" id="postal-code" class="w-full px-3 py-2 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" placeholder="Kode pos">
                           </div>
                       </div>
                       <div class="flex items-center mt-2">
                           <input type="checkbox" id="save-address" class="form-checkbox h-4 w-4 text-blue-600">
                           <label for="save-address" class="ml-2 text-sm text-gray-600">Simpan alamat ini untuk pembelian berikutnya</label>
                       </div>
                   </div>
               </div>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                <!-- Left Side: Cart Items -->
                <div>
                    <h4 class="font-bold text-lg mb-4">Produk di Keranjang</h4>
                    <div class="space-y-3 max-h-64 overflow-y-auto pr-2">
                        <% let checkoutTotal = 0; %>
                        <% if (items && items.length) { %>
                            <% items.forEach(function(it){ %>
                                <% let subtotal = (it.price || 0) * (it.quantity || 1); checkoutTotal += subtotal; %>
                                <div class="flex items-center space-x-3">
                                    <img src="<%= it.image || '/image/katalog.png' %>" alt="<%= it.name %>" class="w-16 h-16 object-cover rounded-md">
                                    <div class="flex-grow">
                                        <p class="font-semibold text-gray-800"><%= it.name %></p>
                                        <p class="text-sm text-gray-500"><%= it.quantity %> x Rp <%= it.price ? it.price.toLocaleString('id-ID') : '0' %></p>
                                    </div>
                                    <p class="font-semibold text-gray-800">Rp <%= subtotal.toLocaleString('id-ID') %></p>
                                </div>
                            <% }) %>
                        <% } %>
                    </div>
                    <div class="border-t mt-4 pt-4">
                        <div class="flex justify-between font-bold text-xl">
                            <span>Total Harga:</span>
                            <span>Rp <%= checkoutTotal.toLocaleString('id-ID') %></span>
                        </div>
                    </div>
                </div>
                <!-- Right Side: Payment Info -->
                <div class="bg-gray-50 p-6 rounded-lg">
                    <h4 class="font-bold text-lg mb-4">Metode Pembayaran</h4>
                    <div class="space-y-4 max-h-48 overflow-y-auto">
                        <div>
                            <label class="flex items-center p-4 border-2 rounded-lg cursor-pointer hover:bg-blue-50 transition-all duration-200 hover:border-blue-500">
                                <input type="radio" name="payment-group" value="e-wallet" class="form-radio h-5 w-5 text-blue-600">
                                <div class="ml-4">
                                    <span class="text-lg font-semibold text-gray-800">E-Wallet</span>
                                    <p class="text-sm text-gray-500">Pembayaran instan melalui e-wallet</p>
                                </div>
                            </label>
                            <div id="e-wallet-options" class="hidden ml-8 mt-4 space-y-4">
                                <label class="flex items-center p-3 border rounded-lg hover:bg-gray-50 transition-colors duration-200">
                                    <input type="radio" name="payment-method" value="DANA" class="form-radio h-4 w-4 text-blue-600">
                                    <div class="ml-3 flex items-center">
                                        <img src="/image/Logo DANA.png" alt="DANA" class="h-8 w-auto object-contain" title="DANA">
                                    </div>
                                </label>
                                <label class="flex items-center p-3 border rounded-lg hover:bg-gray-50 transition-colors duration-200">
                                    <input type="radio" name="payment-method" value="GoPay" class="form-radio h-4 w-4 text-blue-600">
                                    <div class="ml-3 flex items-center">
                                        <img src="/image/Logo GOPAY.png" alt="GoPay" class="h-8 w-auto object-contain" title="GoPay">
                                    </div>
                                </label>
                            </div>
                        </div>
                        <div>
                            <label class="flex items-center p-4 border-2 rounded-lg cursor-pointer hover:bg-blue-50 transition-all duration-200 hover:border-blue-500">
                                <input type="radio" name="payment-group" value="virtual-account" class="form-radio h-5 w-5 text-blue-600">
                                <div class="ml-4">
                                    <span class="text-lg font-semibold text-gray-800">Virtual Account</span>
                                    <p class="text-sm text-gray-500">Transfer bank melalui virtual account</p>
                                </div>
                            </label>
                            <div id="virtual-account-options" class="hidden ml-8 mt-4 space-y-4">
                                <label class="flex items-center p-3 border rounded-lg hover:bg-gray-50 transition-colors duration-200">
                                    <input type="radio" name="payment-method" value="Mandiri VA" class="form-radio h-4 w-4 text-blue-600">
                                    <div class="ml-3 flex items-center">
                                        <img src="/image/Logo Mandiri.png" alt="Mandiri" class="h-8 w-auto object-contain">
                                        <span class="ml-2 text-sm font-medium text-gray-700">Virtual Account Mandiri</span>
                                    </div>
                                </label>
                                <label class="flex items-center p-3 border rounded-lg hover:bg-gray-50 transition-colors duration-200">
                                    <input type="radio" name="payment-method" value="BNI VA" class="form-radio h-4 w-4 text-blue-600">
                                    <div class="ml-3 flex items-center">
                                        <img src="/image/Logo BNI.png" alt="BNI" class="h-8 w-auto object-contain">
                                        <span class="ml-2 text-sm font-medium text-gray-700">Virtual Account BNI</span>
                                    </div>
                                </label>
                                <label class="flex items-center p-3 border rounded-lg hover:bg-gray-50 transition-colors duration-200">
                                    <input type="radio" name="payment-method" value="BCA VA" class="form-radio h-4 w-4 text-blue-600">
                                    <div class="ml-3 flex items-center">
                                        <img src="/image/Logo.png" alt="BCA" class="h-8 w-auto object-contain">
                                        <span class="ml-2 text-sm font-medium text-gray-700">Virtual Account BCA</span>
                                    </div>
                                </label>
                                <label class="flex items-center p-3 border rounded-lg hover:bg-gray-50 transition-colors duration-200">
                                    <input type="radio" name="payment-method" value="BCA VA" class="form-radio h-4 w-4 text-blue-600">
                                    <div class="ml-3 flex items-center">
                                        <img src="/image/Logo BCA.png" alt="BCA" class="h-8 w-auto object-contain">
                                        <span class="ml-2 text-sm font-medium text-gray-700">Virtual Account BCA</span>
                                    </div>
                                </label>
                            </div>
                        </div>
                    </div>
                    <button id="confirm-payment-btn" class="w-full bg-green-600 hover:bg-green-700 text-white font-bold py-3 rounded-lg transition duration-300 transform hover:scale-105 mt-6">
                        Bayar Sekarang
                    </button>
                    <p class="text-xs text-gray-500 mt-3 text-center">Dengan mengklik "Bayar Sekarang", Anda akan diarahkan ke halaman pembayaran.</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Custom Popup Notification (for success/error feedback) -->
    <div id="custom-popup" class="custom-popup">
        <div class="icon"></div>
        <div class="message"></div>
    </div>

</body>
</html>