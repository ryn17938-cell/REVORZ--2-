<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manajemen Pesanan - Admin REVOZ</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/global.min.css">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #fdfbfb,  #f9fdff, #b8dcff, #85dcff);
            min-height: 100vh;
        }
        .table-container {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border-radius: 1.5rem;
            padding: 2rem;
            box-shadow: 0 15px 40px rgba(0,0,0,0.1);
        }
        .status-badge {
            padding: 0.25rem 0.75rem;
            border-radius: 9999px;
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .status-waiting {
            background-color: #fef3c7; /* amber-100 */
            color: #b45309; /* amber-700 */
        }
        .status-approved {
            background-color: #d1fae5; /* green-100 */
            color: #047857; /* green-700 */
        }
        .status-rejected {
            background-color: #fee2e2; /* red-100 */
            color: #b91c1c; /* red-700 */
        }
        .admin-filter-btn {
            padding: 0.5rem 1rem;
            border-radius: 10px;
            font-weight: 600;
            background-color: #e5e7eb; /* gray-200 */
            color: #4b5563; /* gray-600 */
            transition: all 0.2s ease-in-out;
            border: 2px solid transparent;
        }
        .admin-filter-btn:hover {
            background-color: #d1d5db; /* gray-300 */
            color: #1f2937; /* gray-800 */
        }
        .admin-filter-btn.active {
            background-color: #dbeafe; /* blue-100 */
            color: #1d4ed8; /* blue-700 */
            border-color: #93c5fd; /* blue-300 */
            box-shadow: 0 2px 8px rgba(59, 130, 246, 0.2);
        }
        .count-badge {
            display: inline-block;
            margin-left: 0.5rem;
            padding: 0.1rem 0.5rem;
            border-radius: 8px;
            font-size: 0.8em;
            background-color: rgba(0,0,0,0.1);
        }
        .admin-filter-btn.active .count-badge {
            background-color: #1d4ed8; /* blue-700 */
            color: white;
        }
    </style>
</head>
<body class="font-sans text-gray-800 min-h-screen flex flex-col">

    <!-- Header -->
    <header class="bg-white/80 backdrop-blur-md shadow-md py-4">
        <div class="container mx-auto flex justify-between items-center px-4">
            <a href="/" class="flex items-center space-x-2">
                <img src="/image/Logo.png" alt="REVOZ Logo" class="h-14 w-auto">
                <span class="text-2xl font-bold text-blue-600">REVOZ</span>
            </a>
            <nav class="hidden md:flex space-x-6 ml-8">
                <a href="/admin/dashboard" class="text-gray-600 hover:text-blue-600 transition duration-300">Dashboard</a>
                <a href="/admin/orders" class="text-blue-600 font-semibold border-b-2 border-blue-600 transition duration-300">Pesanan</a>
                <a href="/catalog" class="text-gray-600 hover:text-blue-600 transition duration-300">Katalog</a>
                <a href="/category" class="text-gray-600 hover:text-blue-600 transition duration-300">Kategori</a>
                <a href="/admin/add-product" class="text-gray-600 hover:text-blue-600 transition duration-300">Tambah Produk</a>
            </nav>
            <div class="flex-1 flex justify-center px-8">
                <form action="/category" method="get" class="w-full max-w-lg">
                    <div class="relative">
                        <span class="absolute inset-y-0 left-0 pl-3 flex items-center">
                            <svg class="h-5 w-5 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z" clip-rule="evenodd" />
                            </svg>
                        </span>
                        <input class="search-input w-full pl-10 pr-4 py-2 rounded-full text-gray-700 border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500" type="text" name="q" placeholder="Cari produk...">
                    </div>
                </form>
            </div>
            <div class="flex items-center space-x-4">
                <a href="/cart" class="relative text-gray-600 hover:text-blue-600">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 0a2 2 0 100 4 2 2 0 000-4z" /></svg>
                    <span class="absolute -top-2 -right-2 bg-red-500 text-white text-xs rounded-full px-1"><%= cartItemCount %></span>
                </a>
                <% if (isLoggedIn) { %>
                    <div class="flex items-center space-x-2">
                        <% if (profilePicture) { %>
                            <img src="<%= profilePicture %>" alt="Profile" class="w-8 h-8 rounded-full object-cover border-2 border-blue-500">
                        <% } else { %>
                            <div class="w-8 h-8 rounded-full bg-blue-500 flex items-center justify-center text-white font-bold text-sm">
                                <%= username.charAt(0).toUpperCase() %>
                            </div>
                        <% } %>
                        <a href="/profile" class="text-gray-600 hover:text-blue-600 transition duration-300">Halo, <%= username %></a>
                    </div>
                    <a href="/logout" class="text-gray-600 hover:text-blue-600 ml-4">Logout</a>
                <% } else { %>
                    <a href="/login" class="text-gray-600 hover:text-blue-600">
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                        </svg>
                    </a>
                <% } %>
            </div>
        </div>
    </header>

    <main class="container mx-auto py-8 px-4">
        <h1 class="text-3xl font-bold mb-8">Manajemen Pesanan</h1>

        <div class="table-container">
            <!-- Filter and Search -->
            <div class="flex flex-col md:flex-row justify-between items-center mb-6 space-y-4 md:space-y-0">
                <div class="flex flex-wrap gap-3">
                    <a href="/admin/orders" class="admin-filter-btn <%= !filters.status ? 'active' : '' %>">Semua Pesanan</a>
                    <a href="/admin/orders?status=waiting" class="admin-filter-btn <%= filters.status === 'waiting' ? 'active' : '' %>">Menunggu</a>
                    <a href="/admin/orders?status=approved" class="admin-filter-btn <%= filters.status === 'approved' ? 'active' : '' %>">Disetujui</a>
                    <a href="/admin/orders?status=rejected" class="admin-filter-btn <%= filters.status === 'rejected' ? 'active' : '' %>">Ditolak</a>
                </div>
                <form action="/admin/orders" method="get" class="w-full md:w-auto">
                    <div class="relative">
                        <input type="text" name="q" placeholder="Cari Order ID atau Pengguna..." class="w-full pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500" value="<%= filters.q || '' %>">
                        <span class="absolute inset-y-0 left-0 pl-3 flex items-center">
                            <svg class="h-5 w-5 text-gray-400" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z" clip-rule="evenodd" />
                            </svg>
                        </span>
                    </div>
                </form>
            </div>

            <% if (orders && orders.length > 0) { %>
                <div class="overflow-x-auto">
                    <table class="min-w-full divide-y divide-gray-200">
                        <thead class="bg-gray-50">
                            <tr>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Order ID</th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Pengguna</th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Total</th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Tanggal</th>
                                <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Aksi</th>
                            </tr>
                        </thead>
                        <tbody class="bg-white divide-y divide-gray-200">
                            <% orders.forEach(order => { %>
                                <tr>
                                    <td class="px-6 py-4 whitespace-nowrap"><%= order.id %></td>
                                    <td class="px-6 py-4 whitespace-nowrap"><%= order.username %></td>
                                    <td class="px-6 py-4 whitespace-nowrap">Rp <%= order.total.toLocaleString('id-ID') %></td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <span class="status-badge status-<%= order.status %>"><%= order.status %></span>
                                    </td>
                                    <td class="px-6 py-4 whitespace-nowrap"><%= new Date(order.updated_at).toLocaleDateString('id-ID') %></td>
                                    <td class="px-6 py-4 whitespace-nowrap">
                                        <button class="view-details-btn text-blue-600 hover:text-blue-800 font-medium" data-order-id="<%= order.id %>">Lihat Detail</button>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <div class="text-center py-12 text-gray-500">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 mx-auto text-gray-400 mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01" />
                    </svg>
                    <h3 class="mt-2 text-lg font-medium text-gray-800">Tidak Ada Pesanan</h3>
                    <p class="mt-1 text-sm text-gray-600">Tidak ada pesanan yang ditemukan dengan filter ini.</p>
                </div>
            <% } %>
        </div>
    </main>

    <!-- Order Details Modal -->
    <div id="order-details-modal" class="fixed inset-0 bg-gray-900 bg-opacity-75 flex items-center justify-center z-50 hidden">
        <div class="bg-white rounded-lg shadow-xl p-8 m-4 max-w-2xl w-full">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-2xl font-bold text-gray-800">Detail Pesanan #<span id="modal-order-id"></span></h2>
                <button id="close-order-details-modal" class="text-gray-500 hover:text-gray-800 text-3xl">&times;</button>
            </div>
            <div id="modal-order-content">
                <!-- Order details will be loaded here -->
                <p>Loading...</p>
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white py-8 mt-auto">
        <div class="container mx-auto text-center px-4">
            <p>&copy; 2025 REVOZ. Semua Hak Dilindungi.</p>
        </div>
    </footer>

    <script>
        async function postJson(url, body) {
            const res = await fetch(url, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(body)
            });
            return res.json();
        }

        // Order Details Modal Logic
        const orderDetailsModal = document.getElementById('order-details-modal');
        const closeOrderDetailsModalBtn = document.getElementById('close-order-details-modal');
        const modalOrderId = document.getElementById('modal-order-id');
        const modalOrderContent = document.getElementById('modal-order-content');

        document.querySelectorAll('.view-details-btn').forEach(button => {
            button.addEventListener('click', async (e) => {
                const orderId = e.target.dataset.orderId;
                modalOrderId.textContent = orderId;
                modalOrderContent.innerHTML = '<p>Loading...</p>';
                orderDetailsModal.classList.remove('hidden');

                try {
                    const response = await fetch(`/api/admin/orders/${orderId}/details`);
                    const data = await response.json();

                    if (data.ok) {
                        let itemsHtml = '<h3 class="font-semibold text-lg mb-2">Produk:</h3><ul class="space-y-2">';
                        data.items.forEach(item => {
                            itemsHtml += `
                                <li class="flex items-center space-x-3">
                                    <img src="${item.image || '/image/katalog.png'}" alt="${item.name}" class="w-16 h-16 object-cover rounded-md">
                                    <div>
                                        <p class="font-semibold text-gray-800">${item.name} (x${item.quantity})</p>
                                        <p class="text-sm text-gray-600">Warna: ${item.warna}, Ukuran: ${item.ukuran}</p>
                                        <p class="text-sm text-gray-500">Rp ${(item.price * item.quantity).toLocaleString('id-ID')}</p>
                                    </div>
                                </li>
                            `;
                        });
                        itemsHtml += '</ul>';

                        modalOrderContent.innerHTML = `
                            <p class="mb-2"><strong>Pengguna:</strong> ${data.order.username}</p>
                            <p class="mb-2"><strong>Total:</strong> Rp ${data.order.total.toLocaleString('id-ID')}</p>
                            <p class="mb-4"><strong>Status:</strong> <span class="status-badge status-${data.order.status}">${data.order.status}</span></p>
                            ${itemsHtml}
                        `;
                    } else {
                        modalOrderContent.innerHTML = `<p class="text-red-500">${data.message || 'Gagal memuat detail pesanan.'}</p>`;
                    }
                } catch (error) {
                    console.error('Error fetching order details:', error);
                    modalOrderContent.innerHTML = '<p class="text-red-500">Terjadi kesalahan saat memuat detail pesanan.</p>';
                }
            });
        });

        closeOrderDetailsModalBtn.addEventListener('click', () => {
            orderDetailsModal.classList.add('hidden');
        });

        orderDetailsModal.addEventListener('click', (e) => {
            if (e.target === orderDetailsModal) {
                orderDetailsModal.classList.add('hidden');
            }
        });
    </script>

</body>
</html>
